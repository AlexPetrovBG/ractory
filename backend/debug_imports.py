import sys, os; print("Python Path:"); [print(f"  - {path}") for path in sys.path]; print("\nTrying to import routers module:"); try: from app import routers; print("SUCCESS: routers module imported"); print(f"routers.__file__: {routers.__file__}"); try: from app.routers import auth; print("SUCCESS: auth module imported") except ImportError as e: print(f"FAILED to import auth module: {e}") except ImportError as e: print(f"FAILED to import routers module: {e}"); print("\nChecking if files exist:"); [print(f"  - {path}: {\"EXISTS\" if os.path.exists(path) else \"MISSING\"}") for path in ["/app/app/routers", "/app/app/routers/__init__.py", "/app/app/routers/auth.py"]]
